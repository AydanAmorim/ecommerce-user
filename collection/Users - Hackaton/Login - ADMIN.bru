meta {
  name: Login - ADMIN
  type: http
  seq: 1
}

post {
  url: api/autenticacao
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "123"
  }
}

vars:post-response {
  accessToken: res.body.accessToken
}
