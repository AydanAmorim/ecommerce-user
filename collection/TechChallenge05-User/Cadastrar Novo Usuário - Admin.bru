meta {
  name: Cadastrar Novo Usuário - Admin
  type: http
  seq: 5
}

post {
  url: http://localhost:8080/v1/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "username": "aydan.macedo",
    "password": "admin"
  }
}
