meta {
  name: Login - BASIC
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/login
  body: json
  auth: none
}

body:json {
  {
    "username": "aydan.macedo",
    "password": "admin"
  }
}

vars:post-response {
  accessToken: res.body.accessToken
}
